experiment_id: context_trace
benchmarks:
  - name: context_trace
    n_tasks: 20
    trace_lengths: [4, 8, 16, 32, 64]
    seeds: [1]
variants: [self-accept, H1, H2, gated-resample, typed-repair+retain]
model:
  client: dummy
budget:
  max_retries: 1
  veriharness_k: 2
  max_leaf_calls_per_task: 4
