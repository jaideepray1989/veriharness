experiment_id: mini_workflow
benchmarks:
  - name: mini_workflow
    n_tasks: 5
    seeds: [1]
variants: [self-accept, H1, H2, gated-resample, typed-repair+retain]
model:
  client: dummy
budget:
  max_retries: 1
  veriharness_k: 2
  max_leaf_calls_per_task: 4
