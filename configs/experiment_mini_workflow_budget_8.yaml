experiment_id: mini_workflow_budget_8
benchmarks:
  - name: mini_workflow
    n_tasks: 30
    seeds: [1, 2, 3]
variants: [gated-resample, generic-retry, typed-repair+retain]
model:
  client: coreai_freeform
evaluation:
  oracle_guided_acceptance: false
  result_role: primary_oracle_blind_budget_verification
budget:
  max_retries: 7
  veriharness_k: 2
  max_leaf_calls_per_task: 8
  max_wall_time_seconds: 604800
metadata:
  matrix_purpose: mini_workflow_budget_sensitivity_check
  workflow_tasks_per_seed: 30
  call_budget: 8
  parent_matrix: practical_matrix
