experiment_id: provenance_bias
benchmarks:
  - name: provenance_bias
    n_tasks: 20
    seeds: [1]
variants: [H0, H1, H2, H3, H4]
model:
  client: dummy
budget:
  max_retries: 1
  veriharness_k: 2
  max_leaf_calls_per_task: 4
